import { Routes } from '@angular/router';

export const routes: Routes = [
  // Routes will be added here as components are created
  // Example:
  // { path: '', component: SoundboardComponent },
];
